name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # Frontend: Build
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Build Frontend
        working-directory: ./Frontend
        run: |
          npm install
          npm run build

      # Backend: Build
      - name: Setup JDK
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Build Backend
        working-directory: ./Backend
        run: |
          mvn clean package -DskipTests

      

      # Login to ECR
      - name: Login to Amazon ECR
        uses: docker/login-action@v2
        with:
          registry: 221082202241.dkr.ecr.ap-southeast-1.amazonaws.com
          ecr: auto

      # Frontend: Build and Push to ECR
      - name: Build and Push Frontend Docker Image
        working-directory: ./Frontend
        run: |
          docker build -t 221082202241.dkr.ecr.ap-southeast-1.amazonaws.com/aioceaneye/frontend:dev .
          docker push 221082202241.dkr.ecr.ap-southeast-1.amazonaws.com/aioceaneye/frontend:dev

      # Backend: Build and Push to ECR
      - name: Build and Push Backend Docker Image
        working-directory: ./Backend
        run: |
          docker build -t 221082202241.dkr.ecr.ap-southeast-1.amazonaws.com/aioceaneye/backend:dev .
          docker push 221082202241.dkr.ecr.ap-southeast-1.amazonaws.com/aioceaneye/backend:dev

      # Deploy to EC2
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            if ! command -v git &> /dev/null; then
              sudo apt update
              sudo apt install git -y
            fi
            if ! command -v docker-compose &> /dev/null; then
              sudo curl -L "https://github.com/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
              sudo chmod +x /usr/local/bin/docker-compose
            fi
            mkdir -p /home/ubuntu/telemetry-v02
            cd /home/ubuntu/telemetry-v02
            git pull origin main || git clone https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} .
            aws ecr get-login-password --region <region> | docker login --username AWS --password-stdin <account-id>.dkr.ecr.<region>.amazonaws.com
            docker-compose pull
            docker-compose up -d --remove-orphans
            echo "*/5 * * * * /home/ubuntu/telemetry-v02/upload-logs-to-s3.sh" | crontab -
            crontab -l